#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n1,n2,i,j,k;
    cin>>n1>>n2;
    int count=0;
    for( i=n1;i<=n2;i++)
{
    
    k=sqrt(i);
    if((k*k)==i)
    {
        count++;
    }
}   
cout<<count;
}
